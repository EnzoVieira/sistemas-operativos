#include <stdio.h>
#include <unistd.h>

#include "../include/ex1.h"
#include "../include/ex2.h"
#include "../include/ex4.h" // mysystem()

int main() {
  // ex1();
  // ex2();

  // int ret;

  // char command1[] = "ls -l -a -h";
  // ret = mysystem(command1);
  // printf("[father] mysystem() returned %d\n", ret);

  // char command2[] = "sleep 2";
  // ret = mysystem(command2);
  // printf("[father] mysystem() returned %d\n", ret);

  // char command3[] = "ps";
  // ret = mysystem(command3);
  // printf("[father] mysystem() returned %d\n", ret);

  return 0;
}
